version: "3.8"

services:
  web:
    build: .
    image: easecargo-web:latest
    ports:
      - "8000:8000"
    environment:
      - FLASK_ENV=production
    volumes:
      - ./:/app:ro
      - ./instance:/app/instance
    restart: unless-stopped
